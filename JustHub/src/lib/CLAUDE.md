# JustHub/src/lib — utilitário compartilhado

Só o `cn.ts`: helper `cn()` (clsx + tailwind-merge) usado em toda classe condicional do app,
padrão comum a todos os fronts do monorepo. Sempre dar o par `dark:` nas cores ao usar `cn()`
em novas classes.

Ver docs/resumo-projeto.md (seção 2, linha JustHub, e seção 11) para detalhes completos.
