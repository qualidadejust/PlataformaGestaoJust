# JustHub

Portal de entrada da plataforma.

- **Porta**: Front 4500 (sem backend/DB)
- **Stack**: React 19 + Vite 6 (front-only)
- **Papel**: tela única com cards de todos os módulos, mostra status no ar/offline via `/api/health` de cada app — ponto de entrada da plataforma
